// Moved to @/components/ui/StatGrid. Kept as a re-export shim for existing importers.
export { StatGrid, default } from "@/components/ui/StatGrid";
export type { StatGridProps } from "@/components/ui/StatGrid";
