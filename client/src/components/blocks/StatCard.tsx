// Moved to @/components/ui/StatCard. Kept as a re-export shim for existing importers.
export { StatCard, default } from "@/components/ui/StatCard";
export type { StatCardProps } from "@/components/ui/StatCard";
