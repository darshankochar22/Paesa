import { ReportStub } from "../ReportStub";
export default function DebitNoteRegister() {
  return <ReportStub title="Debit Note Register" description="Displays all debit note vouchers (purchase returns / debit memos) in sequence for the selected period." />;
}
