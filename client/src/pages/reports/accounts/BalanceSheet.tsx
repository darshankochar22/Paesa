import { ReportStub } from "../ReportStub";
export default function BalanceSheet() {
  return <ReportStub title="Balance Sheet" description="Displays the financial position of the company — Assets (Fixed + Current) and Liabilities (Capital + Current) — as at the end of the selected period." />;
}
