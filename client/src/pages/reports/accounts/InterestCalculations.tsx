import { ReportStub } from "../ReportStub";
export default function InterestCalculations() {
  return <ReportStub title="Interest Calculations" description="Calculates interest on outstanding receivables and payables based on configured interest parameters and overdue days." />;
}
