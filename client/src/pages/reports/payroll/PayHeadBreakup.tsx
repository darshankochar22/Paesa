import { ReportStub } from "../ReportStub";
export default function PayHeadBreakup() {
  return <ReportStub title="Pay Head Breakup" description="Displays employee-wise breakup of each pay head (Basic, HRA, PF, etc.) for the selected period." />;
}
