import { ReportStub } from "../ReportStub";
export default function NegativeStock() {
  return <ReportStub title="Negative Stock" description="Displays stock items whose quantity has gone below zero — indicating delivery vouchers recorded before the corresponding receipt vouchers." />;
}
