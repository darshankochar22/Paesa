import { ReportStub } from "../ReportStub";
export default function BatchVouchers() {
  return <ReportStub title="Batch Vouchers" description="Displays inventory vouchers (receipts, deliveries, transfers) batch-wise for lot-tracked stock items." />;
}
