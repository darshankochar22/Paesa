import { ReportStub } from "../ReportStub";
export default function StockQuery() {
  return <ReportStub title="Stock Query" description="Provides a quick view of current stock position — quantity on hand, reserved for orders, and available — for any item." />;
}
